-- 0017: горизонтальная раскладка разреза здания.
-- spans_zone_ids — для parking/stylobate/techfloor список id корпусов, над/под/между которыми
-- расположен элемент (мультиселект охвата; допускает несмежный набор). Для building/section пусто.
-- Чисто слой рисования разреза: на локализацию строк сметы, группировку и тиражирование не влияет.
-- Аддитивно и идемпотентно. Старые зоны получают '{}' → рисуются «на всю ширину».
ALTER TABLE project_zones
  ADD COLUMN IF NOT EXISTS spans_zone_ids UUID[] NOT NULL DEFAULT '{}';
