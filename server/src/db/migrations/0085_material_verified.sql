-- 0085: флаг «проверенный материал» в справочнике материалов (курирование каталога).
--   Отдельный смысл, НЕ мягкое удаление: is_active скрывает материал из каталога,
--   is_verified лишь помечает его как проверенный (галочка в блоке справочника на «Смете»
--   и фильтр «только проверенные»). Пока ни на что, кроме отметки и фильтра, не влияет.
-- Аддитивна и идемпотентна (IF NOT EXISTS) — совместима с deploy-estimat --migrate:
--   один батч, чистый SQL без psql-метакоманд.

ALTER TABLE material_catalog
  ADD COLUMN IF NOT EXISTS is_verified BOOLEAN NOT NULL DEFAULT false;
